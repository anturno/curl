node_modules
.env
.env.*
!.env.example
.eve
.vercel
.next
.output
.nitro
dist
.DS_Store
*.tsbuildinfo
.eve/junit.xml
.agents
.claude
.devin
.cursor

# Maintainer-only notes (roadmap, product decisions, drafts).
# Keep internal/README.md tracked so the convention is visible.
/internal/**
!/internal/
!/internal/README.md

# Vercel CLI appends .env*; keep the example tracked.
.env*
!.env.example
