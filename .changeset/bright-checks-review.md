---
"anturno-curl": minor
---

Add GitHub Check Run progress and sticky review summaries, make automatic review explicitly opt-in and repository-scoped, harden the read-only sandbox and configuration validation, and add deterministic test/eval and release gates.
